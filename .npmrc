# Firebase Hosting's web-frameworks integration stages an SSR Cloud Function whose injected
# `firebase-frameworks` shim still peer-requires firebase-admin <=13, while this project runs 14.
# The app imports firebase-admin directly, so the shim's stale peer range is a packaging issue
# rather than a runtime one — allow the install to proceed instead of failing the deploy.
legacy-peer-deps=true
